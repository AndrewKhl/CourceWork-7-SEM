<Window x:Class="MyHotel.RegistrationDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MyHotel"
        mc:Ignorable="d"
        WindowStartupLocation="CenterOwner"
        local:CloseDialog.DialogResult="{Binding Path=IsDialogClose}"
        Title="Registration" Height="350" Width="340">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition Height="24"/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="Name:" Style="{StaticResource CommonTextBlockStyle}"/>
        <TextBox Grid.Row="0" Grid.Column="1" Style="{StaticResource CommonTextBoxStyle}"
                 Text="{Binding Path=Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True, ValidatesOnExceptions=True}"/>

        <TextBlock Grid.Row="1" Grid.Column="0" Text="Last Name:" Style="{StaticResource CommonTextBlockStyle}"/>
        <TextBox Grid.Row="1" Grid.Column="1" Style="{StaticResource CommonTextBoxStyle}"
                 Text="{Binding Path=LastName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True, ValidatesOnExceptions=True}"/>

        <TextBlock Grid.Row="2" Grid.Column="0" Text="Birthday:" Style="{StaticResource CommonTextBlockStyle}"/>
        <DatePicker Grid.Row="2" Grid.Column="1" SelectedDateFormat="Short" Style="{StaticResource CommonDatePickerStyle}"
                    SelectedDate="{Binding Path=Birthday, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True, ValidatesOnExceptions=True}"/>

        <TextBlock Grid.Row="3" Grid.Column="0" Text="Email:" Style="{StaticResource CommonTextBlockStyle}"/>
        <TextBox Grid.Row="3" Grid.Column="1" Style="{StaticResource CommonTextBoxStyle}"
                 Text="{Binding Path=Email, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True, ValidatesOnExceptions=True}"/>

        <TextBlock Grid.Row="4" Grid.Column="0" Text="Password:" Style="{StaticResource CommonTextBlockStyle}"/>
        <TextBox Grid.Row="4" Grid.Column="1" Margin="4" Height="24"
                 Text="{Binding Password, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True, ValidatesOnExceptions=True}">
            <TextBox.Style>
                <Style TargetType="TextBox" BasedOn="{StaticResource CommonTextBoxStyle}">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate>
                                <Grid >
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <TextBox x:Name="passwordBox" BorderThickness="1,1,0,1" VerticalContentAlignment="Center"
                                             Text="{Binding Password, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                             VerticalAlignment="Stretch">
                                        <TextBox.Style>
                                            <Style TargetType="TextBox" BasedOn="{StaticResource CommonTextBoxStyle}">
                                                <Setter Property="Margin" Value="0"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Path=IsPasswordShown, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Value="True">
                                                        <DataTrigger.Setters>
                                                            <Setter Property="FontFamily" Value="Segoe UI"/>
                                                        </DataTrigger.Setters>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Path=IsPasswordShown, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Value="False">
                                                        <DataTrigger.Setters>
                                                            <Setter Property="FontSize" Value="14"/>
                                                            <Setter Property="FontFamily" Value="pack://application:,,,/Resources/Fonts/#password"/>
                                                        </DataTrigger.Setters>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBox.Style>
                                    </TextBox>
                                    <Button Grid.Column="1" BorderThickness="0,1,1,1" x:Name="showPasswordButton" VerticalAlignment="Stretch"
                                            Background="Transparent" Command="{Binding Path=ShowPasswordCommand}" BorderBrush="{DynamicResource {x:Static SystemColors.ActiveBorderBrushKey}}">
                                        <Image Width="18">
                                            <Image.Style>
                                                <Style TargetType="Image">
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Path=IsPasswordShown, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Value="True">
                                                            <DataTrigger.Setters>
                                                                <Setter Property="Source" Value="/Resources/Icons/eye_black.png"/>
                                                            </DataTrigger.Setters>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding Path=IsPasswordShown, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Value="False">
                                                            <DataTrigger.Setters>
                                                                <Setter Property="Source" Value="/Resources/Icons/eye_gray.png"/>
                                                            </DataTrigger.Setters>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Image.Style>
                                        </Image>
                                    </Button>
                                </Grid>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </TextBox.Style>
        </TextBox>

        <TextBlock Grid.Row="5" Grid.Column="0" Text="Confirm Password:" Style="{StaticResource CommonTextBlockStyle}"/>
        <TextBox Grid.Row="5" Grid.Column="1" Margin="4" Height="24"
                 Text="{Binding ConfirmPassword, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True, ValidatesOnExceptions=True}">
            <TextBox.Style>
                <Style TargetType="TextBox" BasedOn="{StaticResource CommonTextBoxStyle}">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate>
                                <Grid >
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <TextBox BorderThickness="1,1,0,1" VerticalContentAlignment="Center"
                                             Text="{Binding ConfirmPassword, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                             VerticalAlignment="Stretch">
                                        <TextBox.Style>
                                            <Style TargetType="TextBox" BasedOn="{StaticResource CommonTextBoxStyle}">
                                                <Setter Property="Margin" Value="0"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Path=IsConfirmPasswordShown, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Value="True">
                                                        <DataTrigger.Setters>
                                                            <Setter Property="FontFamily" Value="Segoe UI"/>
                                                        </DataTrigger.Setters>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Path=IsConfirmPasswordShown, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Value="False">
                                                        <DataTrigger.Setters>
                                                            <Setter Property="FontSize" Value="14"/>
                                                            <Setter Property="FontFamily" Value="pack://application:,,,/Resources/Fonts/#password"/>
                                                        </DataTrigger.Setters>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBox.Style>
                                    </TextBox>
                                    <Button Grid.Column="1" BorderThickness="0,1,1,1" VerticalAlignment="Stretch"
                                            Background="Transparent" Command="{Binding Path=ShowPasswordCommand}" BorderBrush="{DynamicResource {x:Static SystemColors.ActiveBorderBrushKey}}">
                                        <Image Width="18">
                                            <Image.Style>
                                                <Style TargetType="Image">
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Path=IsConfirmPasswordShown, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Value="True">
                                                            <DataTrigger.Setters>
                                                                <Setter Property="Source" Value="/Resources/Icons/eye_black.png"/>
                                                            </DataTrigger.Setters>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding Path=IsConfirmPasswordShown, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Value="False">
                                                            <DataTrigger.Setters>
                                                                <Setter Property="Source" Value="/Resources/Icons/eye_gray.png"/>
                                                            </DataTrigger.Setters>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Image.Style>
                                        </Image>
                                    </Button>
                                </Grid>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </TextBox.Style>
        </TextBox>

        <TextBlock Grid.Row="6" Grid.ColumnSpan="2" Style="{StaticResource CommonTextBlockStyle}" Foreground="Red"
                 HorizontalAlignment="Center" Text="{Binding Path=ErrorText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>

        <Button Grid.Row="7" Grid.ColumnSpan="2" Content="Sign up" Command="{Binding Path=RegistrationCommand}"
                Style="{StaticResource CommonButtonStyle}"/>

        <Button Grid.Row="8" Grid.ColumnSpan="2" Content="Back?" Background="Transparent" BorderThickness="0"
                Style="{StaticResource CommonButtonStyle}" Command="{Binding Path=BackCommand}"/>
    </Grid>
</Window>
