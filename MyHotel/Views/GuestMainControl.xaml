<UserControl x:Class="MyHotel.GuestMainControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MyHotel"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Style="{StaticResource BorderToolBarStyle}">
            <Menu Background="Transparent" HorizontalAlignment="Right">
                <MenuItem Header="{Binding CurrentUser.Name, UpdateSourceTrigger=PropertyChanged}">
                    <MenuItem Header="Your profile"/>
                    <MenuItem Header="Your orders"/>
                    <MenuItem Header="Your services"/>
                    <Separator/>
                    <MenuItem Header="Log out"/>
                </MenuItem>
            </Menu>
        </Border>
    </Grid>
</UserControl>
